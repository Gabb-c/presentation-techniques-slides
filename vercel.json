{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "buildCommand": "pnpm build",
  "installCommand": "pnpm install --frozen-lockfile && pnpm exec playwright install-deps",
  "outputDirectory": "dist"
}